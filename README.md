# Neovim init

Create the folder in {user}/*/ if it doesn't exist.

in nvim, run:
```nvim
:PackerSync
```
then restart nvim.

Have a blast

### Windows Path
```plaintext
{user}\AppData\Local\nvim\init.lua
```


### Ubuntu Path
```plaintext
{user}/.config/nvim/init.lua
```
